"use client";

import ChatTab from "@/components/ChatTab";

export default function ChatPage() {
    // files prop is currently unused by ChatTab but kept for compatibility interfaces
    return <ChatTab files={[]} />;
}
